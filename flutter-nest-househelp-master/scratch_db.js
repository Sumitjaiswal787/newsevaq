const { Client } = require('pg'); const client = new Client({ connectionString: 'postgresql://postgres:Sumit%40787870@127.0.0.1:5432/sevaq_db' }); client.connect().then(() => client.query('SELECT * FROM "Booking" ORDER BY "createdAt" DESC LIMIT 5').then(res => { console.table(res.rows.map(r => ({id: r.id, status: r.status, otp: r.otp, verified: r.isOtpVerified}))); client.end(); }));
