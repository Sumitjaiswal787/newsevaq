const { DataSource } = require('typeorm'); const dataSource = new DataSource({ type: 'postgres', url: 'postgresql://postgres:Sumit%40787870@127.0.0.1:5432/sevaq_db', synchronize: false, entities: [] }); async function main() { await dataSource.initialize(); const workers = await dataSource.query('SELECT w.id as worker_id, u.latitude, u.longitude, w."isAvailable" FROM worker w JOIN "user" u ON w.user_id = u.id'); console.log('Workers:', workers); } main().then(()=>dataSource.destroy());
